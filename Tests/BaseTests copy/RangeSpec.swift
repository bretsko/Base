

import Quick
import Nimble
import XCTest
import Base


class RangeSpec: QuickSpec {
    
    override func spec() {
        describe("Range") {
            
            it("can init") {
                
                //TODO: test all these inits
//                /// from and to can be equal -> can match 1 unit
//                init(_ from: Int, _ to: Int) {
//                    crashIf(from >= to)
//                    self = .rng(from: from, to: to)
//                }
//                init(_ idxSet: Set<Int>) {
//                    crashIf(idxSet.isEmpty)
//                    self = .idxSet(idxSet)
//                }
//
//                //TODO: if contiguous - make rng?
//                init(_ indexes: [Int]) {
//                    self.init(Set(indexes))
//                }
//                init(index: Int) {
//                    self.init([index])
//                }
//
//                /// crashes if start < 0 or length < 0
//                init(start: Int = 0, length: Int) {
//                    crashIfFalse (start >= 0 && length >= 0)
//                    self.init(start, start + length)
//                }
                
                
//                let rng1 = Rng(1,1)
//
//                let rng2 = Rng(0,1)
//
//                let rng3 = Rng(1,2)
//
//                let rng4 = Rng(2,1)
//
//                let rng1 = Rng(1,1)

                //expect().to(contain(" "))
            }
            
            it("can get values from ranges") {
                
//                let rng1 = Rng(1,1)
//
//                let rng2 = Rng(0,1)
     
                
                //expect().to(contain(" "))
            }
            
            it("can manage ranges in Array") {
                
//                let rng1 = Rng(1,1)
//
//                let rng2 = Rng(0,1)
                
                //expect().to(contain(" "))
            }
            
            it("can manage ranges in Set") {
                
//                let rng1 = Rng(1,1)
//
//                let rng2 = Rng(0,1)
   
                
                //expect().to(contain(" "))
            }
            
            it("can translate to Swift Range models ranges in Set") {
                
                // init with it
                // let rng1 = Rng(1,1)
                //
                // let rng2 = Rng(0,1)
                
                // get Range from Rng obj
                
                //expect().to(contain(" "))
            }
        }
    }
}






